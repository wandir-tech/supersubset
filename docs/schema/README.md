# Schema References

This directory contains the human-facing schema references that sit above the generated package artifacts.

Current references:

- `canonical-schema-v0.md` — current dashboard contract highlights, including alerts and interaction navigation

Planned references:

- `canonical-schema-v0.md` — Human-readable schema specification
- `dashboard-definition.json` — JSON Schema for DashboardDefinition
- `examples/` — Example dashboard definitions in JSON and YAML
- `migrations/` — Migration documentation across schema versions
