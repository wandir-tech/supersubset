---
title: Filter Scope
description: Control which widgets a filter affects in your Supersubset dashboard.
sidebar:
  order: 3
---

import ExpandAll from '../../../components/ExpandAll.astro';
import ScreenshotComparison from '../../../components/ScreenshotComparison.astro';
import filter_builder_designer from '../../../assets/screenshots/filters/filter-builder-default-designer.png';
import FeatureScreenshot from '../../../components/FeatureScreenshot.astro';
import filter_scope_multiple_viewer from '../../../assets/screenshots/filters/filter-scope-multiple-active-viewer.png';

# Filter Scope

Filter scope determines which widgets are affected when a viewer changes a filter value. Scope is authored on each dashboard filter from **Dashboard Filters** in the designer header. By default, filters are **global** — they affect all widgets. You can narrow the scope to target specific widgets or pages.

Scope is separate from Filter Bar composition. Scope controls which widgets update; the Filter Bar widget's `Shown Filters` setting controls which authored filters a specific bar displays.

<ExpandAll />

## Scope Types

### Global Scope

The default. A global filter affects every widget on every page of the dashboard.

<ScreenshotComparison
  label="Global filter scope"
  designerSrc={filter_builder_designer}
  designerAlt="Global filter scope in designer mode"
/>

### Widget-Specific Scope

Limit a filter to affect only specific widgets. Useful when different sections of a dashboard use different datasets.

<FeatureScreenshot
  src={filter_scope_multiple_viewer}
  alt="Dashboard with multiple filters active showing scoped effects"
  caption="Multiple filters active — each filter's scope determines which widgets update"
/>

## Scope vs. Shown Filters

- **Scope** decides which widgets respond when a filter value changes.
- **Shown Filters** on a Filter Bar widget decides which authored filters are visible in that bar.
- These settings combine cleanly: one page can have an all-filters bar and a smaller subset bar, while each authored filter still targets only the widgets in its scope.

## Tips

- Start with **global scope** — it works for most dashboards
- Use **widget-specific scope** when you have widgets from different datasets on the same page
- Use a second Filter Bar widget when you want viewers to see only a focused subset of the authored filters
- Cross-filtering (click-based filtering) has its own scope rules — see [Cross-Filtering](/filters/cross-filtering/)

## Related Features

- [Select Filter](/filters/select-filter/) — dropdown filtering
- [Date Filter](/filters/date-filter/) — date range filtering
- [Cross-Filtering](/filters/cross-filtering/) — click-based filtering
