---
title: Gauge
description: Configure gauge charts for single-metric visualization in Supersubset.
sidebar:
  order: 8
---

import ExpandAll from '../../../components/ExpandAll.astro';
import ScreenshotComparison from '../../../components/ScreenshotComparison.astro';
import PropertyShowcase from '../../../components/PropertyShowcase.astro';
import gauge_viewer from '../../../assets/screenshots/chart-types/gauge-default-viewer.png';
import gauge_designer from '../../../assets/screenshots/chart-types/gauge-default-designer.png';
import FeatureScreenshot from '../../../components/FeatureScreenshot.astro';
import gauge_props_designer from '../../../assets/screenshots/chart-types/gauge-props-designer.png';
import gauge_round_cap_before from '../../../assets/screenshots/chart-types/gauge-round-cap-before-viewer.png';
import gauge_round_cap_after from '../../../assets/screenshots/chart-types/gauge-round-cap-viewer.png';
import gauge_round_cap_callout from '../../../assets/screenshots/chart-types/gauge-round-cap-callout-designer.png';
import gauge_progress_before from '../../../assets/screenshots/chart-types/gauge-progress-before-viewer.png';
import gauge_progress_after from '../../../assets/screenshots/chart-types/gauge-progress-viewer.png';
import gauge_progress_callout from '../../../assets/screenshots/chart-types/gauge-progress-callout-designer.png';

# Gauge

Gauges display a single numeric value against a defined range, similar to a speedometer. Ideal for KPIs that need to show progress toward a target or threshold.

<ExpandAll />

## Default Appearance

A gauge with default settings shows a half-circle dial with a needle pointing to the current value.

<ScreenshotComparison
  label="Default gauge"
  designerSrc={gauge_designer}
  designerAlt="Default gauge in designer mode"
  viewerSrc={gauge_viewer}
  viewerAlt="Default gauge in viewer mode"
/>

## Configuration Options

### Value Field (`valueField`)

The numeric field to display on the gauge.

<FeatureScreenshot
  src={gauge_props_designer}
  alt="Value field binding"
  caption="Configure value field binding in the property panel"
/>

### Minimum (`min`)

The minimum value of the gauge range.

<FeatureScreenshot
  src={gauge_props_designer}
  alt="Minimum range setting"
  caption="Configure minimum range setting in the property panel"
/>

### Maximum (`max`)

The maximum value of the gauge range.

<PropertyShowcase
  label="Round cap gauge style"
  beforeSrc={gauge_round_cap_before}
  beforeAlt="Gauge with default flat cap"
  afterSrc={gauge_round_cap_after}
  afterAlt="Gauge with round cap style"
  calloutSrc={gauge_round_cap_callout}
  calloutAlt="Maximum range property highlighted in designer"
/>

### Split Number (`splitNumber`)

Number of segments dividing the gauge arc. More splits give finer graduation marks.

<PropertyShowcase
  label="Progress gauge style"
  beforeSrc={gauge_progress_before}
  beforeAlt="Gauge with default dial style"
  afterSrc={gauge_progress_after}
  afterAlt="Gauge with progress style"
  calloutSrc={gauge_progress_callout}
  calloutAlt="Split number property highlighted in designer"
/>

## Tips

- Set **min** and **max** to match your business context (e.g., 0–100 for percentages)
- Use multiple gauges side by side for a KPI dashboard row
- For simpler metric display, consider a [KPI Card](/widgets/kpi-card/) instead

## Related Features

- [KPI Card](/widgets/kpi-card/) — simpler single-value display
- [Funnel Chart](/chart-types/funnel-chart/) — stage-based metrics
